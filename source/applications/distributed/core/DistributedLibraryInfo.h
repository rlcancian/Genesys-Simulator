#pragma once

#include <string>

namespace genesys::distributed {

/**
 * @brief Returns the human-readable name of the distributed orchestration layer.
 * @details Placeholder entry point that anchors the reusable library target while the
 * orchestration components (discovery, scheduling, execution, aggregation) are added in
 * later tasks.
 */
std::string distributedLayerName();

} // namespace genesys::distributed
