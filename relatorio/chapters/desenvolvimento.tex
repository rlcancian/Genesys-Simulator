\section{Desenvolvimento}

\subsection{Organização do trabalho}

O trabalho foi dividido em nove marcos (\textit{milestones} M1--M9), cada um com escopo bem delimitado e verificado por compilação e teste antes de avançar. A divisão não foi estritamente por pessoa, mas por dependência técnica: os primeiros marcos estabeleceram as interfaces e extensões necessárias; os seguintes adicionaram funcionalidades sobre essa base; M9 fechou com o harness de testes.

Como artefatos do processo, o grupo manteve três documentos ao longo do desenvolvimento. O primeiro é o \texttt{DEVELOPMENT\_DataAnalyzer.md}, um diário técnico que registra, para cada marco, o problema que motivou a mudança, o que foi alterado e as decisões de projeto -- inclusive as que foram deliberadamente rejeitadas, como o uso de \texttt{StatisticsDataFile\_if} (discutido na Seção~\ref{sec:decisoes}). O segundo é a própria suíte de testes do comando \texttt{-{}-demo}, que funciona como especificação executável: ao documentar comportamentos esperados em código, ela serve tanto de validação quanto de referência para quem for manter o componente. O terceiro é o pull request sem conflitos direcionado ao branch \texttt{2026-1}, que inclui todos os arquivos modificados e criados.

A Tabela~\ref{tab:arquivos} lista os arquivos afetados.

\begin{table}[htbp]
\centering
\caption{Arquivos modificados ou criados}
\label{tab:arquivos}
\small
\begin{tabular}{ll}
\toprule
\textbf{Arquivo} & \textbf{Ação} \\
\midrule
\texttt{Fitter\_if.h}              & Adicionado \texttt{setDataValues()} \\
\texttt{FitterDefaultImpl.h}        & Adicionados \texttt{setDataValues()} e \texttt{\_computeStatsFromLoadedData()} \\
\texttt{FitterDummyImpl.h/.cpp}     & Stub vazio para satisfazer a interface \\
\texttt{DataAnalyzer\_if.h}         & Novo -- interface pública e structs de saída \\
\texttt{DataAnalyzerDefaultImpl1.h} & Novo -- declaração da classe concreta \\
\texttt{DataAnalyzerDefaultImpl1.cpp} & Novo -- toda a lógica ($\approx$810 linhas) \\
\texttt{TraitsTools.h}              & Especialização \texttt{TraitsTools<DataAnalyzer\_if>} \\
\texttt{CMakeLists.txt}             & \texttt{DataAnalyzerDefaultImpl1.cpp} adicionado às fontes \\
\texttt{main.cpp}                   & Comando \texttt{-{}-demo} \\
\bottomrule
\end{tabular}

\medskip
\small Todos os arquivos estão em \texttt{source/tools/}.
\end{table}

\subsection{Extensão da interface Fitter\_if (M1)}

\texttt{FitterDefaultImpl} só aceitava dados via arquivo binário. Para que o \texttt{DataAnalyzer} pudesse repassar ao fitter um vetor já em memória, foi preciso adicionar \texttt{setDataValues(const std::vector<double>\&)} tanto à interface \texttt{Fitter\_if} quanto à implementação concreta.

A lógica interna de \texttt{FitterDefaultImpl} foi ligeiramente refatorada: o bloco que calculava estatísticas de cache foi extraído para o método privado \texttt{\_computeStatsFromLoadedData()}, chamado agora tanto pelo caminho de arquivo binário quanto pelo novo caminho em memória. Os flags \texttt{\_cacheLoaded} e \texttt{\_cacheUsable} são definidos por ambos, de modo que o mecanismo de \textit{lazy loading} em \texttt{\_ensureDataLoaded()} enxerga os dados como já carregados e não tenta abrir arquivo algum.

\subsection{Interface DataAnalyzer\_if e structs de saída (M2)}

A interface \texttt{DataAnalyzer\_if} define cinco structs de saída puramente de dados, sem métodos. A arquitetura de classes pode ser resumida pelo diagrama UML a seguir:

\begin{verbatim}
+-----------------------+
|  <<interface>>        |
|  DataAnalyzer_if      |
+-----------------------+
           ^
           | implements
           |
+--------------------------+        +------------------------------+
| DataAnalyzerDefaultImpl1 |------->| HypothesisTesterDefaultImpl1 |
+--------------------------+        +------------------------------+
           |
           | uses
           v
+--------------------------+
|    FitterDefaultImpl     |
+--------------------------+
\end{verbatim}

As structs de saída são:

\begin{itemize}
    \item \texttt{SummaryStatistics} -- n, mín, máx, amplitude, média, mediana, moda, Q1, Q3, variância, desvio-padrão, CV, assimetria, curtose;
    \item \texttt{HistogramData} -- número de classes, limites inferiores, frequências absolutas e relativas;
    \item \texttt{BoxplotData} -- mín, Q1, mediana, Q3, máx, IIQ, lista de outliers;
    \item \texttt{FitResult} -- nome da distribuição, SSE, até cinco parâmetros, flag de validade;
    \item \texttt{GoFResult} -- nome, estatística de teste, p-valor, valor crítico, nível de significância, decisão e conclusão em texto.
\end{itemize}

A grafia americana (\textit{Analyzer}) foi adotada para evitar colisão com o antigo \texttt{DataAnalyser\_if} (grafia britânica), que possui dependência de \texttt{ExperimentManager\_if} e pertence a outra camada arquitetural.

\subsection{Carregamento de dados e estatísticas descritivas (M3)}

O carregamento de dados suporta dois caminhos. Via \texttt{setDataValues(vector<double>)}, os dados são armazenados em memória, o vetor é ordenado em \texttt{\_sortedData} e as estatísticas de cache são computadas. Via \texttt{setDataFilename}, o parser \texttt{SimulationResultsDatasetParser::loadFromTextFile} converte arquivos nos quatro formatos texto do GenESyS (RawNumeric, RecordLegacy, RecordEnriched, GuiTabular) e chama \texttt{setDataValues} em seguida.

Os quantis são calculados pelo método R7 (interpolação linear): dado percentil $p$, calcula-se $h = (n-1)p$, $i = \lfloor h \rfloor$, $f = h - i$, e o resultado é $x_{(i)}(1-f) + x_{(i+1)}f$. Assimetria e curtose são calculadas como momentos padronizados de terceira e quarta ordem, com curtose em excesso (subtraindo 3).

\subsubsection*{Decisão de projeto: StatisticsDataFile\_if}

A proposta sugeria encaminhar as estatísticas descritivas pela interface \texttt{StatisticsDatafile\_if}. Essa interface estende \texttt{CollectorDatafile\_if}, que é estruturalmente acoplada a um arquivo binário de coletor -- não há construtor nem setter que aceite \texttt{vector<double>}. Utilizá-la exigiria escrever os dados em arquivo binário e relê-los, derrotando o propósito de trabalhar em memória. A decisão foi calcular as estatísticas diretamente, produzindo resultados equivalentes sem o custo de I/O adicional.

\subsection{Ajuste de distribuições (M4)}

O método \texttt{fitDistribution(name)} despacha para o método correto de \texttt{FitterDefaultImpl} com base no nome e empacota SSE e parâmetros em \texttt{FitResult}. São suportadas sete distribuições: uniforme, triangular, normal, exponencial, erlang, beta e Weibull.

O método \texttt{fitAll()} chama \texttt{\_fitter.fitAll()} para obter o nome da melhor distribuição e, em seguida, chama \texttt{fitDistribution(nome)} para recuperar os parâmetros. O duplo ajuste é intencional: \texttt{fitAll} retorna apenas nome e SSE, não os parâmetros.

\subsection{Testes de aderência (M5 e M6)}

Para o qui-quadrado, o número de classes segue a fórmula de Sturges ($k = \max(5,\ 1 + \lceil \log_2 n \rceil)$). Classes com frequência esperada $E < 5$ são fundidas com a adjacente. O grau de liberdade é $k_{\text{fundido}} - 1 - p$, onde $p$ é o número de parâmetros estimados a partir dos dados. O p-valor é obtido integrando numericamente a PDF da qui-quadrado via \texttt{SolverDefaultImpl1}; o valor crítico por bissecção sobre a CDF acumulada.

Para o Kolmogorov-Smirnov, a estatística $D_n = \max_i |F_n(x_i) - F(x_i)|$ é calculada sobre as estatísticas de ordem. O p-valor usa a série assintótica de Kolmogorov com 200 termos; o valor crítico usa a fórmula aproximada $D_\alpha \approx \sqrt{-\ln(\alpha/2)/(2n)}$. A aproximação assintótica subestima levemente o p-valor para $n < 35$, o que é aceitável para o contexto.

\subsection{Análise de séries temporais (M7)}

A média móvel usa janela deslizante com soma acumulada em $O(n)$, produzindo $n - w + 1$ valores. A autocorrelação usa o estimador enviesado $\hat{r}(k) = \hat{c}(k)/\hat{c}(0)$, com \texttt{maxLag} silenciosamente limitado a $n-1$ para evitar leitura fora dos limites. O correlograma retorna o mesmo cálculo que a autocorrelação, distinguindo-se apenas conceitualmente -- sua intenção é ser renderizado como gráfico de barras por defasagem.

\subsection{Inferência paramétrica (M8)}

Os métodos de IC e teste de hipóteses delegam para \texttt{HypothesisTesterDefaultImpl1}, usando as estatísticas em cache. Os testes para duas populações requerem que \texttt{loadSecondSample()} tenha sido chamado previamente.

Uma versão inicial de \texttt{testProportionTwoSamples} e \texttt{testVarianceTwoSamples} invocava por engano as sobrecargas de uma população do \texttt{HypothesisTesterDefaultImpl1}, tratando a estatística da segunda amostra como valor da hipótese nula. A correção foi passar \texttt{\_count2} como quarto argumento, selecionando as sobrecargas corretas de duas populações, que já existiam na implementação.

\subsection{Extensões adicionais (M10)}
\label{sec:m10}

Após a conclusão dos marcos M1--M9, cinco extensões foram acrescentadas ao componente, todas alinhadas à diretriz 11.3.1 de diagnósticos adicionais e ajuste de curvas. Nenhuma delas altera a interface já estabilizada; apenas acrescentam métodos novos e dois campos à struct \texttt{FitResult}.

\subsubsection*{RMSE e R\textsuperscript{2} no FitResult}

A struct \texttt{FitResult} passou a expor dois campos de qualidade derivados do SSE:

\begin{itemize}
    \item \texttt{rmse} = $\sqrt{\text{SSE}/n}$ — erro médio por ponto da CDF empírica;
    \item \texttt{r2} = $1 - \text{SSE}/\text{SST}$ — coeficiente de determinação, onde $\text{SST} = \sum_i (p_i - 0{,}5)^2$ e $p_i = (i+0{,}5)/n$ é a probabilidade de plotagem de Hazen.
\end{itemize}

O R\textsuperscript{2} é mais interpretável que o SSE bruto: um valor próximo de 1 indica que a CDF ajustada reproduz bem a CDF empírica. Para os 50 pontos N(5,1) do demo, o ajuste normal obteve $R^2 = 0{,}986$.

\subsubsection*{Teste de Anderson-Darling}

O teste de Anderson-Darling (AD) é mais poderoso que o KS para detectar desvios nas caudas da distribuição, o que é especialmente relevante para dados de saída de simulação com cauda longa. A estatística é:
\[
A^2 = -n - \frac{1}{n}\sum_{i=1}^{n}(2i-1)\bigl[\ln F(x_{(i)}) + \ln(1 - F(x_{(n+1-i)}))\bigr]
\]
corrigida pelo fator de amostra finita de Stephens (1974): $A^2 \leftarrow A^2 \cdot (1 + 4/n - 25/n^2)$. O p-valor usa a aproximação assintótica de Marsaglia \& Marsaglia (2004); o valor crítico é obtido por bissecção sobre a mesma função, monotonamente decrescente.

\subsubsection*{FitReport: relatório unificado de ajuste}

O método \texttt{analyzeFit(name, alpha)} retorna uma struct \texttt{FitReport} que agrega em uma única chamada:

\begin{itemize}
    \item os parâmetros e métricas de qualidade (\texttt{FitResult} com SSE, RMSE e R\textsuperscript{2});
    \item o resultado do qui-quadrado (\texttt{GoFResult});
    \item o resultado do KS (\texttt{GoFResult});
    \item o resultado do Anderson-Darling (\texttt{GoFResult}).
\end{itemize}

Isso é análogo ao que o \textit{Input Analyzer} do Arena apresenta — parâmetros e diagnósticos lado a lado — mas sem qualquer componente gráfico.

\subsubsection*{Detecção de tendência (detectTrend)}

O método \texttt{detectTrend()} retorna uma struct \texttt{TrendDiagnostic} com:

\begin{itemize}
    \item \texttt{hasTrend} — verdadeiro se $|\hat{r}(1)| > 1{,}96/\sqrt{n}$ (ACF do lag 1 acima da banda de Bartlett a 95\%);
    \item \texttt{hasSeasonality} — verdadeiro se algum $|\hat{r}(k)|$, $k > 1$, exceder a banda;
    \item \texttt{trendSlope} e \texttt{trendIntercept} — parâmetros da regressão linear dos valores sobre o índice temporal, por mínimos quadrados ordinários.
\end{itemize}

Para o dado temporal com tendência crescente do demo, o método detectou corretamente \texttt{hasTrend=true} e estimou \texttt{slope} $\approx 0{,}099$ por observação.

\subsubsection*{Probabilidade de excedância (exceedanceProbability / exceedanceCurve)}

O método \texttt{exceedanceProbability(x, name)} retorna $P(X > x) = 1 - F(x)$, onde $F$ é a CDF teórica da distribuição ajustada. O método \texttt{exceedanceCurve(xMin, xMax, n, name)} avalia essa probabilidade em $n$ pontos igualmente espaçados em $[x_{\min}, x_{\max}]$, produzindo a estrutura numérica de uma curva de excedância. Ambos reutilizam o helper \texttt{\_cdfAt} já existente, sem duplicação de lógica.

\subsection{Integração com o simulador}

O componente é registrado no mecanismo \texttt{TraitsTools<T>} do GenESyS:

\begin{lstlisting}[language=C++, caption={Registro em TraitsTools.h}]
template <> struct TraitsTools<DataAnalyzer_if> {
    typedef DataAnalyzerDefaultImpl1 Implementation;
};
\end{lstlisting}

Qualquer módulo do simulador pode obter uma instância concreta via \texttt{TraitsTools<DataAnalyzer\_if>::Implementation} sem depender da classe concreta diretamente. O executável \texttt{genesys\_tools\_application} foi estendido com o comando \texttt{-{}-demo}, descrito na seção de validação.
